name: "Hello_world"

on:
  push:
    branches:
      - master
      - production

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: "Print Hello World"
        run: echo "Hello World"
